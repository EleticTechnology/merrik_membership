<odoo>

    <record id="ir_cron_membership_recurring_invoice" model="ir.cron">
        <field name="name">Membership Recurring Invoices</field>
        <field name="model_id" ref="model_merrikh_membership"/>
        <field name="state">code</field>
        <field name="code">model.cron_membership_recurring_invoices()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
    </record>

</odoo>
